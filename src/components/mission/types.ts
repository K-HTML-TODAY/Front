export interface MissionComponentProps {
  mainText: string;
  buttonBackgroundColor: string;
  link: string;
}

export interface MissionListType {
  type?: string;
  title: string;
  description: string;
  link: string;
}
